## OnInit

1. Refactor `BooksPage`: It should load data in `ngOnInit()`
2. Import `OnInit` and add implements to your class.
3. Implement `ngOnInit()`

## OnDestroy

*Keep in mind: unsubscribe from observables when the component gets destroyed*

1. Refactor components: It should unsubscribe in `ngOnDestroy()`
2. Import `OnDestroy` and add implements to your class.
3. Implement `ngOnDestroy()`
