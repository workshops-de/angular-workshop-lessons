
### Bonus 1: Use an effect
Create an Effect that uses the `books` Signal and prints out the number of books loaded to the console.

### Bonus 2: Use a ComputedSignal
In `BookCardComponent` create a ComputedSignal `title` which uses the InputSignal `content`and returns the `title` in upper case. Use this new Signal in the template. 
