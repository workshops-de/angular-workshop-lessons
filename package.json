{
  "name": "angular-workshop-lessons",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tooling für das Angular-Workshop-Lessons-Repo (Commit-Guard via Husky + commitlint).",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@commitlint/types": "^21.2.0",
    "husky": "^9.1.7"
  }
}
